// The About page is otherwise static markup — this exists only to give it the
// same sidebar chrome (discipline switcher, discipline-preserving links) as
// every other page.
import { initChrome } from "./discipline.js";

initChrome();
