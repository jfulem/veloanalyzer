{
  "name": "veloanalyzer-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "rm -f ../docs/*.js ../docs/*.css ../docs/*.html ../docs/sql-wasm.wasm ../docs/data.db",
    "build": "tsc -noEmit && vite build",
    "preview": "vite preview --port 4173 --outDir ../docs"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "typescript": "^5.7.2",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
