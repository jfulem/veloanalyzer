{
  "name": "veloanalyzer-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "rm -rf ../docs/assets",
    "build": "tsc -noEmit && vite build",
    "preview": "vite preview --port 4173 --outDir ../docs"
  },
  "dependencies": {
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "@types/sql.js": "^1.4.9",
    "typescript": "^5.7.2",
    "vite": "^6.3.5"
  }
}
